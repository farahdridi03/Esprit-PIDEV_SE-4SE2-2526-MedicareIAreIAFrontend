name: Build and deploy Node.js app to Azure Web App - app-frontend-medicareai-2026

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: npm install and build
        run: |
          npm cache clean --force
          npm install
          chmod +x ./node_modules/.bin/ng
          ./node_modules/.bin/ng build --configuration=production
        env:
          NODE_OPTIONS: --openssl-legacy-provider

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: dist/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_D33653C836724A9EA1E6748363237838 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_C7DF53A8BF6045B39F77EA0F9BA6903B }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_D3D01979C75940F3A275CE1CFE437945 }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'app-frontend-medicareai-2026'
          slot-name: 'Production'
          package: .
